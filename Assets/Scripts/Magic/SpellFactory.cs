using UnityEngine;

public class SpellFactory : MonoBehaviour
{
    //public CompiledSpell CreateSpell(SpellGridSlot[,] grid)
   // {
    //    CompiledSpell spell = new CompiledSpell();

    //    Vector2Int corePos = FindCore(grid);
    //}
    //Vector2Int FindCore(SpellGridSlot[,] grid)
    //{
        
    //}
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
